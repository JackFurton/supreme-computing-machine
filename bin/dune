; bin/dune -- Build config for our executable(s).
;
; (libraries dns_parser) makes our library available to the binary.
; This is how dune wires up dependencies between parts of your project.

(executable
 (name hello)
 (public_name hello)
 (libraries dns_parser))
